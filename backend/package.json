{
  "name": "pokerkings-backend",
  "version": "1.0.0",
  "description": "Backend for PokerKings game",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "migrate": "sequelize-cli db:migrate",
    "migrate:prod": "sequelize-cli db:migrate --env production",
    "migrate:status": "sequelize-cli db:migrate:status --env production",
    "migrate:undo": "sequelize-cli db:migrate:undo:all"
  },
  "keywords": ["poker", "game", "backend"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "sequelize": "^6.35.0",
    "pg": "^8.11.0",
    "socket.io": "^4.6.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "sequelize-cli": "^6.6.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
